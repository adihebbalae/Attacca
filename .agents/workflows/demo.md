---
description: "2-minute live demo of Attacca. Shows the full PRD → agents → build pipeline using a sample project. Writes no files."
---

# Demo

Live walkthrough of Attacca for someone seeing it for the first time. No files written — narrated simulation only.

## Steps

Follow `claude-plugin/commands/demo.md` exactly — same script, same flow.

The only difference: in Antigravity, slash commands are invoked as `/demo` directly from the command palette.
