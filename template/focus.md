# Focus

**One performance.** This session's rendering of the project — where the work got
to, what's next, what stopped it. Rewritten by `/wrap-session` every session;
nothing here is permanent. Anything that should constrain the *next* session
belongs in the score (`CONTEXT.md`, under Key Decisions), not here.

Deploy to `.attacca/focus.md`. **Commit it** — a teammate or cloud agent picking
up the repo reads this file first. If your `.gitignore` ignores `.attacca/`
wholesale, narrow it to `.attacca/audits/` and `.attacca/sbom/`.

Last updated: <!-- date -->

## Current Focus
<!-- What is being built right now. 1-3 bullets. -->

## Next Steps
<!-- Queued work, in order. Prune when stale. -->

## Blockers
<!-- Anything waiting on a human decision or external dependency. "None" if clear. -->
